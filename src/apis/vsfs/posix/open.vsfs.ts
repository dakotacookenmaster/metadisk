/**
 * A POSIX-like function to open a file (and potentially create it)
 * @param pathname The path at which to open (or create) the file
 * @param flags 
 * @param mode 
 */
export default function open(pathname: string, flags: number, mode: string) {

}