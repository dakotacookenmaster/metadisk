import FolderCopyIcon from "@mui/icons-material/FolderCopy"

export const VSFSIcon = (props: any) => {
    return (
        <FolderCopyIcon
            {...props}
            sx={{
                color: "#F1D592",
            }}
        />
    )
}
