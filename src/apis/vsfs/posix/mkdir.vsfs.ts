/**
 * A POSIX-like function to create a directory at the given pathname
 * @param pathname The name of the path at which you want to create the directory
 */
export default function mkdir(pathname: string) {

}